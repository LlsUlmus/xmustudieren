# Android实验项目流程图说明

本文档提供使用Mermaid语法绘制的流程图，可在支持Mermaid的Markdown查看器中显示。

## 第一次Android实验流程图

### 1. ActionBarDemo流程图

```mermaid
graph TD
    A[应用启动] --> B[MainActivity.onCreate]
    B --> C[加载布局文件]
    B --> D[获取ActionBar实例]
    D --> E[设置标题: 华清远见]
    D --> F[初始隐藏ActionBar]
    B --> G[启动ActionBarHideAndShow线程]
    G --> H[线程循环执行]
    H --> I{每500ms执行一次}
    I --> J[count递减]
    J --> K{count == 0?}
    K -->|是| L[切换HIDE/SHOW状态]
    K -->|否| I
    L --> M[handler.sendEmptyMessage]
    M --> N[Handler.handleMessage]
    N --> O{消息类型?}
    O -->|HIDE| P[actionBar.hide]
    O -->|SHOW| Q[actionBar.show]
    P --> I
    Q --> I
```

### 2. ActivityCommunication流程图

```mermaid
sequenceDiagram
    participant U as 用户
    participant M as MainActivity
    participant A1 as Activity1
    participant A2 as Activity2
    
    U->>M: 点击Button1
    M->>A1: Intent + startActivityForResult
    A1->>A1: 显示输入界面
    U->>A1: 输入数据并点击OK
    A1->>A1: setResult(RESULT_OK, Intent)
    A1->>M: finish()
    M->>M: onActivityResult()
    M->>M: 提取URI数据
    M->>M: 更新TextView显示
    
    U->>M: 点击Button2
    M->>A2: Intent + startActivityForResult
    A2->>A2: 显示界面
    U->>A2: 点击返回
    A2->>A2: setResult(RESULT_CANCELED)
    A2->>M: finish()
    M->>M: onActivityResult()
```

### 3. SQLiteExam2数据库操作流程图

```mermaid
graph TD
    A[应用启动] --> B[MainActivity.onCreate]
    B --> C[创建MyDatabaseHelper]
    C --> D{数据库是否存在?}
    D -->|否| E[调用onCreate]
    D -->|是| F[直接打开数据库]
    E --> G[执行CREATE TABLE]
    G --> H[创建STUDENTINFO表]
    H --> F
    F --> I[获取SQLiteDatabase对象]
    I --> J[initData: 插入数据]
    J --> K[创建ContentValues]
    K --> L[db.insert]
    L --> M[数据库插入记录]
    M --> N[db.rawQuery: 查询数据]
    N --> O[返回Cursor对象]
    O --> P[遍历Cursor]
    P --> Q[moveToNext]
    Q --> R[提取列数据]
    R --> S[转换为HashMap]
    S --> T[添加到List]
    T --> U{是否最后一行?}
    U -->|否| Q
    U -->|是| V[创建SimpleAdapter]
    V --> W[绑定到ListView]
    W --> X[显示数据]
```

### 4. GraphicAnimation动画流程图

```mermaid
graph TD
    A[用户点击按钮] --> B[onClick被调用]
    B --> C{模式判断}
    C -->|XML模式| D[调用xml方法]
    C -->|代码模式| E[调用static方法]
    D --> F[AnimationUtils.loadAnimation]
    F --> G[解析XML文件]
    G --> H[创建Animation对象]
    H --> I[mImageView.startAnimation]
    E --> J[new Animation子类]
    J --> K[设置动画参数]
    K --> L[setDuration]
    L --> I
    I --> M[动画开始执行]
    M --> N[根据插值器计算属性值]
    N --> O[更新ImageView属性]
    O --> P{动画是否结束?}
    P -->|否| N
    P -->|是| Q[动画完成]
```

### 5. NoteApp应用流程图

```mermaid
graph TD
    A[应用启动] --> B[MainActivity.onCreate]
    B --> C[初始化数据库]
    B --> D[初始化RecyclerView]
    B --> E[加载笔记列表]
    E --> F[queryAllFromDb]
    F --> G[显示笔记]
    G --> H{用户操作}
    H -->|点击添加| I[Intent启动AddActivity]
    H -->|搜索| J[queryFromDbByTitle]
    H -->|切换布局| K[setToLinearList/setToGridList]
    I --> L[AddActivity显示]
    L --> M[用户输入标题和内容]
    M --> N[点击保存]
    N --> O[insertData插入数据库]
    O --> P[finish返回MainActivity]
    P --> Q[onResume刷新列表]
    J --> R[更新列表显示]
    K --> S[切换布局模式]
```

## 第二次Android实验流程图

### 1. LED灯控制流程图

```mermaid
graph TD
    A[用户点击LED按钮] --> B[MainActivity.onClick]
    B --> C[更新UI图标]
    B --> D[led.open]
    D --> E[JNI调用]
    E --> F[native-lib.cpp: open函数]
    F --> G[open系统调用]
    G --> H[打开/dev/leds_ctl]
    H --> I[返回文件描述符fd]
    I --> J[led.LedOn1/LedOff1]
    J --> K[JNI调用]
    K --> L[native-lib.cpp: LedOn1函数]
    L --> M[ioctl系统调用]
    M --> N[内核空间]
    N --> O[LED字符设备驱动]
    O --> P[解析ioctl命令]
    P --> Q[控制GPIO寄存器]
    Q --> R[设置GPIO引脚电平]
    R --> S[LED硬件状态改变]
    S --> T[led.close]
    T --> U[close系统调用]
    U --> V[关闭设备文件]
```

### 2. 温度采集流程图

```mermaid
sequenceDiagram
    participant U as 用户
    participant M as MainActivity
    participant T as TimeThread
    participant H as Handler
    participant JNI as JNI层
    participant D as 驱动层
    
    U->>M: 点击开始按钮
    M->>JNI: tmp.open
    JNI->>D: 打开温度传感器设备
    M->>T: 启动TimeThread
    loop 循环采集
        T->>T: Thread.sleep(200ms)
        T->>H: sendMessage
        H->>JNI: tmp.read
        JNI->>D: 读取温度值
        D-->>JNI: 返回温度数据
        JNI-->>H: 返回double值
        H->>M: 更新TextView显示
    end
    U->>M: 点击关闭按钮
    M->>JNI: tmp.close
    JNI->>D: 关闭设备
    M->>T: sensorflag = false
    T->>T: 退出循环
```

### 3. 串口通信流程图

```mermaid
graph TD
    A[应用启动] --> B[初始化UI组件]
    B --> C[用户选择串口和波特率]
    C --> D[点击打开串口]
    D --> E[com.open]
    E --> F[JNI调用]
    F --> G[打开串口设备文件]
    G --> H[配置串口参数]
    H --> I[启动MyThread接收线程]
    I --> J[循环读取数据]
    J --> K[com.read阻塞读取]
    K --> L{读取到数据?}
    L -->|是| M[转换为String]
    L -->|否| N[退出循环]
    M --> O[runOnUiThread更新UI]
    O --> J
    C --> P[用户输入数据]
    P --> Q[点击发送]
    Q --> R[com.write]
    R --> S[JNI调用]
    S --> T[write系统调用]
    T --> U[数据写入串口设备]
    U --> V[通过串口硬件发送]
    C --> W[点击关闭串口]
    W --> X[com.close]
    X --> Y[关闭串口设备]
    Y --> N
```

## Android与Linux驱动对比流程图

### Android方式架构

```mermaid
graph TD
    A[Java应用层<br/>MainActivity.java] --> B[JNI接口层<br/>LED.java]
    B --> C[JNI实现层<br/>native-lib.cpp]
    C --> D[Linux系统调用层]
    D --> E[open/ioctl/close]
    E --> F[内核空间]
    F --> G[LED字符设备驱动]
    G --> H[GPIO控制]
    H --> I[硬件层<br/>LED灯]
```

### Linux原生方式架构

```mermaid
graph TD
    A[C应用层<br/>led_app.c] --> B[Linux系统调用层]
    B --> C[open/ioctl/close]
    C --> D[内核空间]
    D --> E[LED字符设备驱动]
    E --> F[file_operations结构体]
    F --> G[led_open/led_ioctl/led_release]
    G --> H[GPIO操作函数]
    H --> I[gpio_request/gpio_set_value]
    I --> J[硬件层<br/>LED灯]
```

### 对比流程图

```mermaid
graph LR
    subgraph Android["Android方式"]
        A1[Java代码] --> A2[JNI桥接]
        A2 --> A3[C++代码]
        A3 --> A4[系统调用]
        A4 --> A5[驱动层]
    end
    
    subgraph Linux["Linux方式"]
        L1[C代码] --> L2[系统调用]
        L2 --> L3[驱动层]
    end
    
    A5 --> H[硬件GPIO]
    L3 --> H
    
    style A1 fill:#e1f5ff
    style A2 fill:#ffe1f5
    style A3 fill:#fff5e1
    style L1 fill:#e1ffe1
```

## 使用说明

这些流程图使用Mermaid语法编写，可以在以下环境中查看：

1. **GitHub/GitLab**：直接支持Mermaid渲染
2. **VS Code**：安装Mermaid Preview插件
3. **在线工具**：https://mermaid.live/
4. **Typora**：内置Mermaid支持

如果您的Markdown查看器不支持Mermaid，可以参考主报告中的文本流程图描述。


