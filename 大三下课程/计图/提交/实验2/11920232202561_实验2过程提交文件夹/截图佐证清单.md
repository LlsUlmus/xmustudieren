# 实验2 截图佐证清单（按报告模板）

本清单用于你写报告时快速定位“关键代码截图”和“运行结果截图”。  
代码文件均在本文件夹内，不会影响你原目录下已完成文件。

---

## Task1：Sierpinski镂垫程序

### 1. 不同三角形不同颜色
- **建议截图代码**（`exp02_task1.cpp`）：
  - `HSV2RGB(...)` 颜色映射函数
  - `drawTriangle(...)` 里 `depth == 0` 时根据三角形中心点计算颜色并 `glColor3f(...)`
- **建议运行结果截图**：
  - 程序静止某一帧，能看到不同层级三角形颜色有差异

### 2. 镂垫动画（颜色变化 + 旋转 + 缩放）
- **建议截图代码**（`exp02_task1.cpp`）：
  - `display()` 中：`glRotatef(...)`、`glScalef(...)`
  - `idle()` 中：`g_timeSec`、`g_rotDeg`、`g_scale` 的更新逻辑
- **建议运行结果截图**：
  - 连续两到三帧（或一张动态录屏截图）体现颜色变化
  - 一张体现旋转角度变化
  - 一张体现缩放变化（大小明显不同）

---

## Task2：完成一个三维程序

### 1. 设置相机位置和投影矩阵
- **建议截图代码**（`exp2-2-1.cpp` 与 `exp02_task2.cpp`）：
  - `reshape(...)` 中 `glMatrixMode(GL_PROJECTION)` + `gluPerspective(...)` 或 `glOrtho(...)`
  - `display()` 中 `gluLookAt(...)`
- **建议运行结果截图**：
  - 调整参数前后至少两张图（例如视角更远/更近，视野更大/更小）

### 2. 对程序进行改写

#### a) 深度测试对比（开/关）
- **建议截图代码**（`exp02_task2.cpp`）：
  - `display()` 中 `glEnable(GL_DEPTH_TEST)` / `glDisable(GL_DEPTH_TEST)` 的分支
  - `glClear(GL_COLOR_BUFFER_BIT | GL_DEPTH_BUFFER_BIT)`
- **建议运行结果截图**：
  - `T` 切换前后各一张（关闭深度测试时遮挡错误更明显）

#### b) 立方体自转而非相机转
- **建议截图代码**（`exp02_task2.cpp`）：
  - `idle()` 中 `g_cubeRot` 更新
  - `display()` 中 `glRotatef(g_cubeRot, ...)` 对立方体模型旋转
- **建议运行结果截图**：
  - 连续两帧（立方体姿态变化，而相机位置不变）

#### c) 交互式相机控制（WASD/QE/鼠标/L）
- **建议截图代码**（`exp02_task2.cpp`）：
  - `keyboardDown(...)` / `keyboardUp(...)`
  - `idle()` 中按键状态驱动相机位移（WASD/QE）
  - `mouseButton(...)` + `passiveMotion(...)`（鼠标朝向控制）
  - `g_lockCamera`（L 锁定逻辑）
- **建议运行结果截图**：
  - 相机前进后退前后对比
  - 鼠标改变朝向前后对比
  - 按 `L` 锁定后“输入无效”的状态图

#### 思考题：如何实现前后遮挡
- **建议截图代码**（`exp02_task2.cpp`）：
  - `glEnable(GL_DEPTH_TEST)`
  - `glClear(... | GL_DEPTH_BUFFER_BIT)`
- **报告答题关键点**：
  - 深度缓冲记录每个像素最近深度值
  - 深度测试比较当前片元与已有深度，保留更近片元，实现正确遮挡

---

## Task3：绘制线框球体

### 1. 不使用glut对象函数绘制线框球
- **建议截图代码**（`exp02_task3.cpp`）：
  - `drawWireSphere(...)` 中经线/纬线循环
  - 证明未使用 `glutWireSphere`
- **建议运行结果截图**：
  - 线框球整体图（与老师示意图一致）

### 2. 添加动画效果（绕圆心旋转）
- **建议截图代码**（`exp02_task3.cpp`）：
  - `idle()` 中 `g_spinDeg` 更新
  - `display()` 中 `glRotatef(...)`
- **建议运行结果截图**：
  - 前后两帧对比旋转姿态

### 3. reshape保持比例
- **建议截图代码**（`exp02_task3.cpp`）：
  - `reshape(...)` 中 `glViewport(...)` 与 `gluPerspective(...)`
- **建议运行结果截图**：
  - 改变窗口大小后的球体图，证明未拉伸变形

### 4. 相机交互控制（同Task2）
- **建议截图代码**（`exp02_task3.cpp`）：
  - `keyboard(...)`、`passiveMotion(...)`、相机方向向量计算
- **建议运行结果截图**：
  - 平移和视角旋转前后对比图

---

## 提交前核对
- 代码：`exp02_task1.cpp`、`exp02_task2.cpp`、`exp02_task3.cpp`
- 可执行：`exp02_task1.exe`、`exp02_task2.exe`、`exp02_task3.exe`
- 参考改写起点：`exp2-2-1.cpp`
- 报告：你的 `*.docx`

